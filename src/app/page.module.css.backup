/* Modern GetShortzy Landing Page - 2025 Style */

.main {
  background-color: #0B0A0F;
  color: #fff;
  overflow-x: hidden;
}

.sectionTitle {
  font-size: 2.5rem;
  font-weight: 700;
  text-align: center;
  margin-bottom: 3rem;
  background: -webkit-linear-gradient(45deg, #a993fe, #7e61e7);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* Hero Section */
.hero {
  padding: 6rem 2rem;
  text-align: center;
  background-image: radial-gradient(circle at 50% 0%, rgba(126, 97, 231, 0.3) 0%, rgba(11, 10, 15, 0) 40%);
}

.heroContent {
  max-width: 800px;
  margin: 0 auto;
}

.heroBadge {
  display: inline-block;
  padding: 0.5rem 1.5rem;
  border-radius: 2rem;
  background-color: rgba(255, 255, 255, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.2);
  margin-bottom: 1.5rem;
  font-size: 0.9rem;
}

.heroTitle {
  font-size: 4rem;
  font-weight: 800;
  margin-bottom: 1.5rem;
  line-height: 1.2;
}

.heroSubtitle {
  font-size: 1.25rem;
  color: #a09fb1;
  max-width: 600px;
  margin: 0 auto 2rem;
  line-height: 1.6;
}

.heroCta {
  display: flex;
  gap: 1rem;
  justify-content: center;
  margin-bottom: 1rem;
}

.ctaBtn {
  padding: 1rem 2rem;
  border-radius: 0.75rem;
  font-size: 1.1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
  text-decoration: none;
}

.primaryBtn {
  background: #7e61e7;
  color: #fff;
  border: none;
}

.primaryBtn:hover {
  background: #6c52c6;
  transform: translateY(-2px);
}

.secondaryBtn {
  background: transparent;
  color: #fff;
  border: 1px solid #a09fb1;
}

.secondaryBtn:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: #fff;
}

.heroNote p {
  font-size: 0.9rem;
  color: #a09fb1;
}

.heroImage {
  margin-top: 4rem;
}

.videoPlaceholder {
  max-width: 800px;
  margin: 0 auto;
  border-radius: 1rem;
  overflow: hidden;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
}

.videoPlaceholder img {
  width: 100%;
  height: auto;
}

/* How It Works Section */
.howItWorks {
  padding: 6rem 2rem;
}

.stepsGrid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.stepCard {
  background: #16151A;
  padding: 2rem;
  border-radius: 1rem;
  text-align: center;
  border: 1px solid #2a2832;
}

.stepNumber {
  font-size: 1.5rem;
  font-weight: 700;
  color: #7e61e7;
  margin-bottom: 1rem;
}

.stepIcon {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.stepTitle {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
}

.stepDescription {
  color: #a09fb1;
  line-height: 1.6;
}

/* Features Section */
.features {
  padding: 6rem 2rem;
}

.featuresGrid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
  gap: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.featureCard {
  background: #16151A;
  padding: 2rem;
  border-radius: 1rem;
  border: 1px solid #2a2832;
}

.featureIcon {
  font-size: 2rem;
  margin-bottom: 1rem;
}

.featureTitle {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
}

.featureDescription {
  color: #a09fb1;
  line-height: 1.6;
}

/* Testimonials Section */
.testimonials {
  padding: 6rem 2rem;
}

.testimonialsGrid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
  gap: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.testimonialCard {
  background: #16151A;
  padding: 2rem;
  border-radius: 1rem;
  border: 1px solid #2a2832;
}

.testimonialText {
  font-size: 1.1rem;
  line-height: 1.6;
  margin-bottom: 1.5rem;
}

.testimonialAuthor {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.authorAvatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
}

.authorName {
  font-weight: 600;
}

.authorHandle {
  color: #a09fb1;
}

/* Pricing Section */
.pricing {
  padding: 6rem 2rem;
}

.pricingGrid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
  max-width: 1000px;
  margin: 0 auto;
  align-items: center;
}

.pricingCard {
  background: #16151A;
  padding: 2.5rem;
  border-radius: 1rem;
  border: 1px solid #2a2832;
  text-align: center;
}

.pricingCardPopular {
  border-color: #7e61e7;
  transform: scale(1.05);
  position: relative;
}

.popularBadge {
  position: absolute;
  top: -1rem;
  left: 50%;
  transform: translateX(-50%);
  background: #7e61e7;
  color: #fff;
  padding: 0.5rem 1rem;
  border-radius: 2rem;
  font-size: 0.9rem;
  font-weight: 600;
}

.pricingTitle {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 1rem;
}

.pricingPrice {
  font-size: 3rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}

.pricingPeriod {
  color: #a09fb1;
  margin-bottom: 2rem;
}

.pricingFeatures {
  list-style: none;
  padding: 0;
  margin-bottom: 2rem;
  text-align: left;
}

.pricingFeatures li {
  margin-bottom: 1rem;
  display: flex;
  align-items: center;
}

.pricingFeatures li::before {
  content: '✓';
  color: #7e61e7;
  margin-right: 0.5rem;
}

.pricingBtn {
  width: 100%;
  display: block;
}

/* FAQ Section */
.faq {
  padding: 6rem 2rem;
}

.faqGrid {
  max-width: 800px;
  margin: 0 auto;
}

.faqItem {
  border-bottom: 1px solid #2a2832;
  padding: 1.5rem 0;
}

.faqQuestion {
  font-size: 1.25rem;
  font-weight: 600;
  margin-bottom: 1rem;
}

.faqAnswer {
  color: #a09fb1;
  line-height: 1.6;
}

/* CTA Section */
.ctaSection {
  padding: 6rem 2rem;
  text-align: center;
  background: linear-gradient(45deg, #a993fe, #7e61e7);
}

.ctaTitle {
  font-size: 3rem;
  font-weight: 700;
  margin-bottom: 1rem;
}

.ctaSubtitle {
  font-size: 1.25rem;
  margin-bottom: 2rem;
}

.ctaBtnLarge {
  padding: 1.25rem 2.5rem;
  font-size: 1.25rem;
}

@media (max-width: 768px) {
  .heroTitle {
    font-size: 2.5rem;
  }

  .heroCta {
    flex-direction: column;
  }

  .ctaBtn {
    width: 100%;
  }
}

